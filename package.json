{
  "name": "aha-looped-transformer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "node scripts/validate.mjs && node scripts/readme.mjs && astro build",
    "preview": "astro preview --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs"
  },
  "dependencies": {
    "astro": "7.3.2",
    "yaml": "2.9.1"
  }
}
