import { defineConfig } from 'astro/config';
export default defineConfig({
  site: 'https://interactive-active-ai.github.io',
  base: '/Aha-Looped-Transformer',
  output: 'static',
  trailingSlash: 'always'
});
